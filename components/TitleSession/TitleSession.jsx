import React from 'react'

const TitleSession = ({ title }) => (
  <div className="max-w-2xl mx-auto py-8 md:max-w-none">
    <h2 className="text-2xl md:text-title text-gray-700 font-cartagena"> {title}</h2>
  </div>
)

export default TitleSession